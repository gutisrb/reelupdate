# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Make.com Webhooks for Image Generation (Shared across all users)
# These are used in Stage Studio for image-to-image generation
VITE_MAKE_CREATE_URL=https://hook.eu2.make.com/your-create-webhook-id
VITE_MAKE_STATUS_URL=https://hook.eu2.make.com/your-status-webhook-id

# Make.com API Key (for webhook authentication if needed)
VITE_MAKE_API_KEY=your-api-key-here

# NOTE: Video generation webhooks are stored per-user in Supabase profiles.webhook_url
# This allows each client to have their own custom Make.com webhook endpoint